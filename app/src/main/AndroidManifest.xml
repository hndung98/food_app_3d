<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sorimachi.fastfoodapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FastFoodApp">
        <activity android:name=".shop.ShopBillManagerActivity"></activity>
        <activity android:name=".customer.CustomerProfileActivity" />
        <activity android:name=".shop.ShopProfileActivity" />
        <activity android:name=".shop.ShopFoodBillActivity" />
        <activity android:name=".shop.ShopFoodManagerActivity" />
        <activity android:name=".shop.ShopAddFoodActivity" />
        <activity android:name=".customer.CustomerMainActivity" />
        <activity android:name=".customer.CustomerFoodListActivity" />
        <activity android:name=".shop.ShopMainActivity" />
        <activity android:name=".customer.HomeUserActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>