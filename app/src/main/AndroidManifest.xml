<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sorimachi.fastfoodapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FastFoodApp">
        <activity android:name=".seller.SellerAddFoodActivity"></activity>
        <activity android:name=".user.CustomerMainActivity" />
        <activity android:name=".user.CustomerFoodListActivity" />
        <activity android:name=".seller.SellerMainActivity"/>
        <activity android:name=".user.HomeUserActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>